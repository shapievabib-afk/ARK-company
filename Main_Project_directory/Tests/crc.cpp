#include "crc.h"

// TODO: реализация Халида (алгоритм CRC16 по протоколу устройства)
// Заглушка — всегда возвращает 0, тесты будут падать пока не реализовано
uint16_t calculateCRC16(const uint8_t* data, size_t length) {
    // Реализовать здесь
    (void)data;
    (void)length;
    return 0x0000;
}
